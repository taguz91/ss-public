export interface Calificacion {
    id_calificacion: number;
    // Objeto de la clase Cliente
    id_cliente: any;
    // On¡bjeto de la clase Producto
    id_producto: any;
    calificacion: number;
    cal_activo: boolean;
}
